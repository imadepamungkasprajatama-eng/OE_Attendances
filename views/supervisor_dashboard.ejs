<%- include('partials/header',{user:user, title:'Attendances Dashboard', isBusy: locals.isBusy, statusText:
  locals.statusText}) %>

  <div class="d-flex justify-content-between align-items-center mb-4">
    <a href="/" class="btn btn-outline-secondary btn-sm rounded-pill px-3">
      ← Back to Home
    </a>
  </div>

  <% if (typeof query !=='undefined' && query && query.msg) { %>
    <div class="mb-4">
      <% if (query.msg==='userUpdated' ) { %>
        <div class="alert alert-success border-0 shadow-sm rounded-3">User data updated successfully.</div>
        <% } else if (query.msg==='userDeleted' ) { %>
          <div class="alert alert-success border-0 shadow-sm rounded-3">User and attendance history deleted
            successfully.</div>
          <% } else if (query.msg==='userNotFound' ) { %>
            <div class="alert alert-warning border-0 shadow-sm rounded-3">User not found.</div>
            <% } else if (query.msg==='error' ) { %>
              <div class="alert alert-danger border-0 shadow-sm rounded-3">An error occurred.</div>
              <% } %>
    </div>
    <% } %>

      <div class="row">
        <div class="col-md-12">
          <div class="card border-0 mb-4 bg-primary text-white"
            style="border-radius: 1rem; background: linear-gradient(135deg, var(--primary-color) 0%, #3730a3 100%);">
            <div class="card-body p-4 p-md-5">
              <h2 class="fw-bold mb-2">Division Dashboard</h2>
              <p class="mb-0 opacity-75">Overview of attendance for <%= monthLabel %>
              </p>
            </div>
          </div>

          <div class="card border-0 shadow-lg" style="margin-top: -3rem; border-radius: 1rem;">
            <div class="card-body p-4">

              <!-- Controls -->
              <div class="d-flex justify-content-between align-items-end">
                <% if (canManageUsers) { %>
                  <div class="d-flex gap-2">
                    <button class="btn btn-outline-dark rounded-pill" data-bs-toggle="modal"
                      data-bs-target="#scheduleModal">
                      <i class="fas fa-calendar-alt me-1"></i> Staff Schedule
                    </button>
                    <button class="btn btn-primary rounded-pill px-4" data-bs-toggle="modal"
                      data-bs-target="#editUserModal" onclick="window.prepareCreateUser()">
                      + New User
                    </button>
                  </div>

                  <!-- MODAL: Staff Schedule (Restored) -->
                  <style>
                    #pills-tab-sup .nav-link.active {
                      color: #fff !important;
                      font-weight: bold;
                    }

                    #pills-tab-sup .nav-link {
                      color: #6c757d;
                    }
                  </style>
                  <div class="modal fade" id="scheduleModal" tabindex="-1" aria-hidden="true">
                    <div class="modal-dialog modal-dialog-centered modal-lg">
                      <div class="modal-content border-0 shadow-lg" style="border-radius: 1rem;">
                        <div class="modal-header border-0 pb-0">
                          <div>
                            <h5 class="modal-title fw-bold">Staff Schedule Management</h5>
                          </div>
                          <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                        </div>
                        <div class="modal-body">
                          <ul class="nav nav-pills mb-3" id="pills-tab-sup" role="tablist">
                            <li class="nav-item" role="presentation">
                              <button class="nav-link active" id="pills-today-tab-sup" data-bs-toggle="pill"
                                data-bs-target="#pills-today-sup" type="button" role="tab">Today's Overview</button>
                            </li>
                            <li class="nav-item" role="presentation">
                              <button class="nav-link" id="pills-sat-tab-sup" data-bs-toggle="pill"
                                data-bs-target="#pills-sat-sup" type="button" role="tab">Saturday Attendance</button>
                            </li>
                            <li class="nav-item" role="presentation">
                              <button class="nav-link" id="pills-hols-tab-sup" data-bs-toggle="pill"
                                data-bs-target="#pills-hols-sup" type="button" role="tab">Holidays</button>
                            </li>
                          </ul>
                          <div class="tab-content" id="pills-tabContent-sup">
                            <!-- TODAY'S OVERVIEW TAB -->
                            <div class="tab-pane fade show active" id="pills-today-sup" role="tabpanel">
                              <!-- Division Selector & Controls -->
                              <form action="/supervisor" method="GET" class="mb-3 d-flex align-items-center gap-2">
                                <label class="fw-bold small text-muted">Division:</label>
                                <select name="division" class="form-select form-select-sm" style="width: auto;"
                                  onchange="this.form.submit()">
                                  <% managedDivisions.forEach(div=> { %>
                                    <option value="<%= div %>" <%=division===div ? 'selected' : '' %>>
                                      <%= div %>
                                    </option>
                                    <% }) %>
                                </select>
                                <input type="hidden" name="month" value="<%= monthParam %>">
                                <input type="hidden" name="openModal" value="true">
                                <button type="submit" class="btn btn-sm btn-outline-primary">Refresh</button>
                              </form>

                              <div class="table-responsive" style="max-height: 400px; overflow-y: auto;">
                                <table class="table table-hover align-middle">
                                  <thead class="bg-light sticky-top">
                                    <tr>
                                      <th>Name</th>
                                      <th>Division</th>
                                      <th>Status</th>
                                      <th>Work Timer</th>
                                      <th>Break Timer</th>
                                    </tr>
                                  </thead>
                                  <tbody>
                                    <% memberSummaries.forEach(summary=> { %>
                                      <tr>
                                        <td>
                                          <div class="d-flex align-items-center">
                                            <div
                                              class="bg-light rounded-circle d-flex align-items-center justify-content-center me-2"
                                              style="width: 32px; height: 32px; font-weight: bold; color: #555;">
                                              <%= summary.user.name.charAt(0).toUpperCase() %>
                                            </div>
                                            <div>
                                              <div class="fw-bold">
                                                <%= summary.user.name %>
                                              </div>
                                              <small class="text-muted">
                                                <%= summary.user.email %>
                                              </small>
                                            </div>
                                          </div>
                                        </td>
                                        <td><span class="badge bg-light text-dark border">
                                            <%= summary.user.division %>
                                          </span></td>
                                        <td>
                                          <% if (summary.todayStats.status==='working' ) { %>
                                            <span class="badge bg-success">Working</span>
                                            <% } else if (summary.todayStats.status==='break' ) { %>
                                              <span class="badge bg-warning">Break</span>
                                              <% } else { %>
                                                <span class="badge bg-secondary">Idle</span>
                                                <% } %>
                                        </td>
                                        <td>
                                          <span class="live-timer-work"
                                            data-base-seconds="<%= summary.todayStats.workSeconds %>"
                                            data-start-time="<%= summary.todayStats.currentWorkStart ? summary.todayStats.currentWorkStart.getTime() : '' %>">
                                            <%= summary.todayStats.workText %>
                                          </span>
                                        </td>
                                        <td>
                                          <span class="live-timer-break"
                                            data-base-seconds="<%= summary.todayStats.breakSeconds %>"
                                            data-start-time="<%= summary.todayStats.currentBreakStart ? summary.todayStats.currentBreakStart.getTime() : '' %>">
                                            <%= summary.todayStats.breakText %>
                                          </span>
                                        </td>
                                      </tr>
                                      <% }) %>
                                  </tbody>
                                </table>
                              </div>
                            </div>

                            <!-- SATURDAY REPORT TAB -->
                            <div class="tab-pane fade" id="pills-sat-sup" role="tabpanel">
                              <form action="/supervisor" method="GET" class="mb-3 d-flex align-items-center gap-2">
                                <% if(typeof division !=='undefined' && division) { %>
                                  <input type="hidden" name="division" value="<%= division %>">
                                  <% } %>
                                    <input type="hidden" name="openModal" value="true">
                                    <label class="fw-bold small text-muted">Month:</label>
                                    <input type="month" name="month" class="form-control form-control-sm"
                                      style="width: auto;" value="<%= monthParam %>">
                                    <button type="submit" class="btn btn-sm btn-outline-primary">Refresh</button>
                              </form>
                              <div class="table-responsive" style="max-height: 400px; overflow-y: auto;">
                                <table class="table table-hover align-middle">
                                  <thead class="bg-light sticky-top">
                                    <tr>
                                      <th>Name</th>
                                      <th>Division</th>
                                      <th class="text-center">Total Hours (Sat)</th>
                                      <th>Report</th>
                                    </tr>
                                  </thead>
                                  <tbody>
                                    <% if(saturdaySummaries && saturdaySummaries.length) { %>
                                      <% saturdaySummaries.forEach(function(ms) { const u=ms.user; if
                                        (ms.hasSaturdayAttendance) { %>
                                        <tr>
                                          <td>
                                            <div class="d-flex flex-column">
                                              <span class="fw-bold small">
                                                <%= u.name %>
                                              </span>
                                              <span class="text-muted" style="font-size: 0.75rem;">
                                                <%= u.email %>
                                              </span>
                                            </div>
                                          </td>
                                          <td><span class="badge bg-light text-dark border">
                                              <%= u.division %>
                                            </span></td>
                                          <td class="text-center fw-bold">
                                            <%= ms.saturdayHoursStr || '00:00:00' %>
                                          </td>
                                          <td>
                                            <a href="/admin/attendance/export-saturday?userId=<%= u._id %>&month=<%= monthParam %>"
                                              class="btn btn-sm btn-outline-primary rounded-pill px-3">Download
                                              (.xlsx)</a>
                                          </td>
                                        </tr>
                                        <% } }); %>
                                          <% if (!saturdaySummaries.some(ms=> ms.hasSaturdayAttendance)) { %>
                                            <tr>
                                              <td colspan="4" class="text-center text-muted">No Saturday attendance
                                                found for this month.</td>
                                            </tr>
                                            <% } %>
                                              <% } else { %>
                                                <tr>
                                                  <td colspan="4" class="text-center text-muted">No users found.</td>
                                                </tr>
                                                <% } %>
                                  </tbody>
                                </table>
                              </div>
                            </div>
                            <div class="tab-pane fade" id="pills-hols-sup" role="tabpanel">
                              <div class="mb-4">
                                <label class="form-label fw-bold small text-muted">Add New Holiday</label>
                                <form action="/admin/holidays/add" method="POST" class="d-flex gap-2">
                                  <input type="date" name="date" class="form-control" required>
                                  <button class="btn btn-primary text-nowrap">Add Date</button>
                                </form>
                              </div>
                              <hr>
                              <h6 class="fw-bold small text-muted mb-3">Upcoming Holidays</h6>
                              <% const holidays=(typeof settings !=='undefined' && settings.holidays) ?
                                settings.holidays : []; %>
                                <% if (holidays.length> 0) { %>
                                  <div class="d-flex flex-wrap gap-2">
                                    <% holidays.forEach(h=> { %>
                                      <div class="badge bg-light text-dark border p-2 d-flex align-items-center gap-2">
                                        <span style="font-size: 1rem;">
                                          <%= h %>
                                        </span>
                                        <form action="/admin/holidays/remove" method="POST" class="d-inline m-0 p-0">
                                          <input type="hidden" name="date" value="<%= h %>">
                                          <button type="submit" class="btn-close"
                                            style="width: 0.5em; height: 0.5em;"></button>
                                        </form>
                                      </div>
                                      <% }) %>
                                  </div>
                                  <% } else { %>
                                    <p class="text-muted small">No holidays set.</p>
                                    <% } %>
                            </div>
                          </div>
                        </div>
                      </div>
                    </div>
                  </div>




              </div>
            </div>
          </div>
        </div>
        <% } %>
      </div>

      <div class="row g-3 align-items-end mb-4">
        <div class="col-md-6">
          <label class="form-label small text-muted fw-bold">Select Division</label>
          <% if (managedDivisions && managedDivisions.length> 1) { %>
            <form method="GET" action="/supervisor">
              <input type="hidden" name="month" value="<%= monthParam %>">
              <select name="division" class="form-select" onchange="this.form.submit()">
                <% managedDivisions.forEach(function(d){ %>
                  <option value="<%= d %>" <%=d===division ? 'selected' : '' %>><%= d %>
                  </option>
                  <% }) %>
              </select>
            </form>
            <% } else { %>
              <div class="form-control bg-light">
                <%= division || '-' %>
              </div>
              <% } %>
        </div>
        <div class="col-md-6">
          <form class="row g-2" method="GET" action="/supervisor">
            <input type="hidden" name="division" value="<%= division %>">
            <div class="col-8">
              <label class="form-label small text-muted fw-bold">Month</label>
              <input type="month" name="month" class="form-control" value="<%= monthParam %>">
            </div>
            <div class="col-4 d-grid">
              <button class="btn btn-primary">Refresh</button>
            </div>
          </form>
        </div>
      </div>

      <!-- Table -->
      <div class="table-responsive">
        <table class="table table-hover align-middle">
          <thead class="bg-light text-muted small text-uppercase">
            <tr>
              <th class="ps-3 border-0 rounded-start">Name</th>
              <th class="border-0">Email</th>
              <th class="border-0">Division</th>
              <th class="border-0">Total Hours</th>
              <th class="pe-3 border-0 rounded-end text-end">Report</th>
              <% if (canManageUsers) { %>
                <th class="border-0 text-center">Actions</th>
                <% } %>
            </tr>
          </thead>
          <tbody>
            <% if (memberSummaries && memberSummaries.length) { %>
              <% memberSummaries.forEach(ms=> { %>
                <tr>
                  <td class="ps-3 fw-medium">
                    <%= ms.user.name %>
                  </td>
                  <td class="text-muted">
                    <%= ms.user.email || '—' %>
                  </td>
                  <td>
                    <span class="badge bg-soft text-dark border">
                      <%= ms.user.division || '-' %>
                    </span>
                  </td>
                  <td>
                    <span class="d-inline-block px-2 py-1 rounded bg-light fw-bold text-dark">
                      <%= ms.workText %>
                    </span>
                  </td>
                  <td class="text-end pe-3">
                    <form method="GET" action="/supervisor/attendance/export-user-month" class="d-inline">
                      <input type="hidden" name="userId" value="<%= ms.user._id %>">
                      <input type="hidden" name="month" value="<%= monthParam %>">
                      <button class="btn btn-sm btn-outline-primary rounded-pill px-3">
                        Download (.xlsx)
                      </button>
                    </form>
                  </td>
                  <% if (canManageUsers) { %>
                    <td class="text-center pe-3 text-end">
                      <button class="btn btn-sm btn-icon btn-white border shadow-sm me-1" data-bs-toggle="modal"
                        data-bs-target="#editUserModal" data-mode="edit" data-id="<%= ms.user._id %>"
                        data-name="<%= ms.user.name %>" data-email="<%= ms.user.email %>"
                        data-division="<%= ms.user.division %>"
                        data-secondary-division="<%= ms.user.secondaryDivision || '' %>"
                        data-access="<%= ms.user.canAccessSupervisorDashboard ? 'true' : 'false' %>"
                        data-work-hours="<%= ms.user.durationWorkHours %>"
                        data-break-minutes="<%= ms.user.durationBreakMinutes %>"
                        data-shift-group="<%= ms.user.shiftGroup || '' %>" title="Edit">
                        Edit
                      </button>

                      <form method="POST" action="/admin/delete-user" style="display:inline"
                        onsubmit="return confirm('Yakin ingin menghapus user ini beserta semua history absennya?');">
                        <input type="hidden" name="userId" value="<%= ms.user._id %>">
                        <button class="btn btn-sm btn-icon btn-white border shadow-sm text-danger" title="Delete">
                          Del
                        </button>
                      </form>
                    </td>
                    <% } %>
                </tr>
                <% }) %>
                  <% } else { %>
                    <tr>
                      <td colspan="5" class="text-center py-5 text-muted">
                        <div class="mb-2" style="font-size: 2rem;">📂</div>
                        No members found or no data relative to this month.
                      </td>
                    </tr>
                    <% } %>
          </tbody>
        </table>
      </div>

      </div>
      </div>
      </div>
      </div>

      <!-- MODAL CREATE / EDIT USER (Shared Style) -->
      <% if (canManageUsers) { %>
        <div class="modal fade" id="editUserModal" tabindex="-1" aria-labelledby="editUserModalLabel"
          aria-hidden="true">
          <div class="modal-dialog modal-dialog-centered">
            <form method="POST" id="userForm" action="/admin/create-user" class="modal-content border-0 shadow-lg">
              <div class="modal-header border-0 pb-0">
                <h5 class="modal-title fw-bold" id="editUserModalLabel">Create User</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
              </div>
              <div class="modal-body pt-4">
                <input type="hidden" name="userId" id="userIdInput">

                <div class="mb-3">
                  <label class="form-label small fw-bold text-muted">Full Name</label>
                  <input name="name" id="nameInput" class="form-control" required placeholder="e.g. John Doe">
                </div>
                <div class="mb-3">
                  <label class="form-label small fw-bold text-muted">Email Address</label>
                  <input name="email" id="emailInput" class="form-control" required placeholder="e.g. john@example.com">
                </div>

                <div class="row mb-3">
                  <div class="col-md-6">
                    <label class="form-label small fw-bold text-muted">Role</label>
                    <select name="role" id="roleInput" class="form-select">
                      <option>Staff</option>
                      <option>Supervisor</option>
                      <option>Operational Manager</option>
                      <option>General Manager</option>
                      <option>Admin</option>
                    </select>
                  </div>
                  <div class="col-md-6">
                    <label class="form-label small fw-bold text-muted">Main Division</label>
                    <select name="division" id="divisionInput" class="form-select">
                      <option value="GM">General Manager(GM)</option>
                      <option value="Admin">Admin</option>
                      <option value="OC">ONEderland Consulting (OC)</option>
                      <option value="N1">Neurone (N1)</option>
                      <option value="SnG">Strategy and Growth (SnG)</option>
                      <option value="e1">Education ONE (e1)</option>
                      <option value="CE">Carbonenergy (CE)</option>
                      <option value="EC">English Cafe (EC)</option>
                      <option value="PX">PeraONE Experience (PX)</option>
                      <option value="FN">Finance (FN)</option>
                      <option value="HR">Human Resource (HR)</option>
                      <option value="All Division">All Division</option>
                    </select>
                  </div>
                </div>

                <div class="row mb-3">
                  <div class="col-md-6">
                    <label class="form-label small fw-bold text-muted">Work Hours (daily)</label>
                    <input type="number" name="durationWorkHours" id="workHoursInput" class="form-control" value="8"
                      required>
                  </div>
                  <div class="col-md-6">
                    <label class="form-label small fw-bold text-muted">Break Duration (minutes)</label>
                    <input type="number" name="durationBreakMinutes" id="breakMinutesInput" class="form-control"
                      value="60" required>
                  </div>
                </div>

                <div class="mb-3">
                  <div class="d-flex justify-content-between">
                    <label class="form-label small fw-bold text-muted">Secondary Division (Optional)</label>
                    <button type="button" class="btn btn-link btn-sm p-0 text-decoration-none" id="addDivisionBtn">+
                      Add</button>
                  </div>
                  <div class="d-none" id="secondaryDivisionWrapper">
                    <select name="secondaryDivision" id="secondaryDivisionInput" class="form-select">
                      <option value="">— Select Additional Division —</option>
                      <option value="Admin">Admin</option>
                      <option value="General Manager">General Manager(GM)</option>
                      <option value="OC">ONEderland Consulting (OC)</option>
                      <option value="N1">Neurone (N1)</option>
                      <option value="SnG">Strategy and Growth (SnG)</option>
                      <option value="e1">Education ONE (e1)</option>
                      <option value="CE">Carbonenergy (CE)</option>
                      <option value="EC">English Cafe (EC)</option>
                      <option value="PX">PeraONE Experience (PX)</option>
                      <option value="FN">Finance (FN)</option>
                      <option value="HR">Human Resource (HR)</option>
                      <option value="All Division">All Division</option>
                    </select>
                  </div>
                </div>
              </div>
              <div class="modal-footer border-0 pt-0">
                <button type="button" class="btn btn-light text-muted" data-bs-dismiss="modal">Cancel</button>
                <button class="btn btn-primary px-4 rounded-pill" type="submit" id="userSubmitBtn">Save User</button>
              </div>
            </form>
          </div>
        </div>
        <% } %>

          <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
          <script>
            (function () {
              const modalEl = document.getElementById('editUserModal');
              const form = document.getElementById('userForm');
              const idInput = document.getElementById('userIdInput');
              const nameInput = document.getElementById('nameInput');
              const emailInput = document.getElementById('emailInput');
              const roleInput = document.getElementById('roleInput');
              const divisionInput = document.getElementById('divisionInput');
              const secondaryDivisionInput = document.getElementById('secondaryDivisionInput');
              const secondaryDivisionWrapper = document.getElementById('secondaryDivisionWrapper');
              const addDivisionBtn = document.getElementById('addDivisionBtn');
              const workHoursInput = document.getElementById('workHoursInput');
              const breakMinutesInput = document.getElementById('breakMinutesInput');
              const titleEl = document.getElementById('editUserModalLabel');

              // dipanggil saat klik tombol "Create User"
              window.prepareCreateUser = function () {
                if (!form) return;
                form.action = '/admin/create-user';
                idInput.value = '';
                nameInput.value = '';
                emailInput.value = '';
                roleInput.value = 'Staff';
                if (divisionInput) divisionInput.value = 'OC';
                if (secondaryDivisionInput) secondaryDivisionInput.value = '';
                if (secondaryDivisionWrapper) secondaryDivisionWrapper.classList.add('d-none');
                if (workHoursInput) workHoursInput.value = '8';
                if (breakMinutesInput) breakMinutesInput.value = '60';
                if (titleEl) titleEl.textContent = 'Create User';
              };

              if (modalEl) {
                modalEl.addEventListener('show.bs.modal', function (event) {
                  const button = event.relatedTarget;
                  if (!button) return;
                  const mode = button.getAttribute('data-mode');

                  if (mode === 'edit') {
                    const id = button.getAttribute('data-id');
                    const name = button.getAttribute('data-name') || '';
                    const email = button.getAttribute('data-email') || '';
                    const role = button.getAttribute('data-role') || 'Staff';
                    const division = button.getAttribute('data-division') || 'OC';
                    const secondaryDivision = button.getAttribute('data-secondary-division') || '';
                    const workHours = button.getAttribute('data-work-hours') || '8';
                    const breakMinutes = button.getAttribute('data-break-minutes') || '60';

                    form.action = '/admin/update-user';
                    idInput.value = id;
                    nameInput.value = name;
                    emailInput.value = email;
                    roleInput.value = role;

                    if (divisionInput) divisionInput.value = division;

                    if (secondaryDivisionInput && secondaryDivisionWrapper) {
                      if (secondaryDivision) {
                        secondaryDivisionInput.value = secondaryDivision;
                        secondaryDivisionWrapper.classList.remove('d-none');
                      } else {
                        secondaryDivisionInput.value = '';
                        secondaryDivisionWrapper.classList.add('d-none');
                      }
                    }

                    if (workHoursInput) workHoursInput.value = workHours;
                    if (breakMinutesInput) breakMinutesInput.value = breakMinutes;

                    if (titleEl) titleEl.textContent = 'Edit User';
                  } else {
                    window.prepareCreateUser();
                  }
                });

                if (addDivisionBtn && secondaryDivisionWrapper) {
                  addDivisionBtn.addEventListener('click', function () {
                    secondaryDivisionWrapper.classList.remove('d-none');
                  });
                }
              }
            })();
          </script>

          <% if (typeof query !=='undefined' && query.openModal) { %>
            <script>
              document.addEventListener('DOMContentLoaded', function () {
                console.log("Auto-opening modal...");
                var modalEl = document.getElementById('scheduleModal');
                if (modalEl) {
                  var myModal = new bootstrap.Modal(modalEl);
                  myModal.show();
                } else {
                  console.error("Modal element not found!");
                }
              });
            </script>
            <% } %>

              <!-- Real-Time Timer Script for Staff Modal -->
              <script>
                function formatTime(sec) {
                  sec = Math.floor(sec);
                  if (sec < 0) sec = 0;
                  const h = Math.floor(sec / 3600);
                  const m = Math.floor((sec % 3600) / 60);
                  const s = sec % 60;
                  return [h, m, s].map(v => v.toString().padStart(2, '0')).join(':');
                }

                function updateStaffTimers() {
                  const workTimers = document.querySelectorAll('.live-timer-work');
                  const breakTimers = document.querySelectorAll('.live-timer-break');
                  const now = Date.now();

                  workTimers.forEach(el => {
                    const base = parseFloat(el.getAttribute('data-base-seconds')) || 0;
                    const start = el.getAttribute('data-start-time');
                    if (start && start !== '') {
                      const diff = (now - parseInt(start)) / 1000;
                      el.innerText = formatTime(base + diff);
                    }
                  });

                  breakTimers.forEach(el => {
                    const base = parseFloat(el.getAttribute('data-base-seconds')) || 0;
                    const start = el.getAttribute('data-start-time');
                    if (start && start !== '') {
                      const diff = (now - parseInt(start)) / 1000;
                      el.innerText = formatTime(base + diff);
                    }
                  });
                }

                // Update every second
                setInterval(updateStaffTimers, 1000);
                updateStaffTimers(); // Initial call
              </script>

              <%- include('partials/footer') %>